import { Component } from '@angular/core';

/** §15.3 — écran d'atterrissage après connexion (détaillé en G3/§7). */
@Component({
  selector: 'app-dashboard',
  template: `<h1>Tableau de bord</h1>`,
})
export class DashboardComponent {}

spring:
  liquibase:
    enabled: true

management:
  server:
    port: 0

security:
  jwt:
    secret: test-only-secret-never-used-in-production-please-change-me

notification:
  mode: log

logging:
  level:
    com.obvgestion: INFO
    org.testcontainers: INFO
